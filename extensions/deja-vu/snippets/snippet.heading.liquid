{% if title or subheading %}
  <div class="deja-vu-head">
    {% if title %}
      <h2>{{ title }}</h2>
    {% endif %}
    {% if subheading %}
      <p>{{ subheading }}</p>
    {% endif %}
  </div>
{% endif %}
