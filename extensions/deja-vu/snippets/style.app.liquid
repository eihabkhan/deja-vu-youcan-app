{% render 'style.typography' %}
{% render 'style.vars' %}

<style>
  deja-vu {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: var(--heading-bottom-spacing);
    
    .head {
      background-color: red;
    }
  }
</style>